node_modules/
.env
# built SPA output (Vite writes here); web/public/ is source and IS tracked
/public/

# receipt/chain dumps written by the verifier (root level only)
/*.json
!package.json
!tsconfig.json

# private design system — see README (vendor it or install with GITHUB_TOKEN)
web/vendor/
*.tsbuildinfo
